# Set the default firewall policies to deny incoming traffic and allow outgoing traffic.
sudo ufw default deny incoming
sudo ufw default allow outgoing

# Allow SSH (port 22), HTTP (port 80), and HTTPS (port 443) traffic through the firewall.
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443

# Enable the firewall to start enforcing the configured rules.
sudo ufw enable
